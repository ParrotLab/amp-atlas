# README assets

| File | Used for | Status |
|---|---|---|
| `atlas-logo-dark.png` | Logo on light backgrounds (GitHub light theme) | Done (from the app's bundled logo) |
| `atlas-logo-light.svg` | Logo on dark backgrounds (GitHub dark theme) | Done (from the app's bundled logo) |
| `atlas-hero.png` | Hero screenshot/GIF under the title | **Needed** — drop a product screenshot here (~1600px wide, displays at 820px) before launch |

The README swaps the logo automatically by theme via a `<picture>` element. Until `atlas-hero.png` exists, the hero image in the README will show as broken — add it before the repo goes public (Task 7 launch pre-flight).
